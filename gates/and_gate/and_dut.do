vlib work
vlog and_dut.v +acc
vsim work.and_tb 
add wave -r *
run -all

# vlib work
# vlog jerky.v tb_jerky.v +acc -->vlog <lead dut><top dut name > <test bench> +acc
# vsim work.tb 
# add wave -r *
# run -all
